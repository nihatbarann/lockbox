{
  "name": "lockbox-web",
  "version": "1.0.0",
  "description": "Lockbox - Enterprise Password Manager (Web Version)",
  "private": true,
  "scripts": {
    "install:all": "npm install && cd server && npm install && cd ../client && npm install --legacy-peer-deps",
    "setup": "npm run install:all && cd server && npm run build",
    "dev": "concurrently -k \"npm:server\" \"npm:client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "build": "cd server && npm run build && cd ../client && npm run build",
    "start": "concurrently -k \"npm:server\" \"npm:client\"",
    "clean": "rimraf node_modules server/node_modules server/dist client/node_modules client/build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "rimraf": "^5.0.10"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
